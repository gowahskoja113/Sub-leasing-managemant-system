# Báo cáo BE — 19/08/2026

Năm phiếu cùng ngày. Chi tiết từng phiếu nằm ở file `BE-DONE-*` tương ứng.

---

## 1. Host xem hợp đồng như Admin

File: `BE-DONE-host-xem-hop-dong-nhu-admin-2026-08-19.md`

| Việc | Kết quả |
|---|---|
| A — Host (`OWNER`) xem / list / tải file HĐ như admin | Xong — không còn 403 do thiếu role + `getHighestRole` nhầm TENANT |
| B — Tên khách HĐ nháp trên DTO host | Xong — fallback `draftTenantName` |
| B2 — `lessorName` không còn = tên toà | Xong — `null` |
| C — Master lease bỏ số bịa | Xong — bỏ 4 field hard-code; trả `contractCode`, `contractScanUrl`, `totalRentAmount` |

**FE:** drawer chi tiết Host không cần sửa. List Host có thể đổi sang `GET /tenant-contracts`. Master lease: field cũ `deposit`/`paymentDay` không còn trên response.

---

## 2. Đổi quản lý khu vực — thông báo & bàn giao

File: `BE-DONE-doi-quan-ly-khu-vuc-thong-bao-va-ban-giao-2026-08-19.md`

| Việc | Kết quả |
|---|---|
| 1 — Thông báo khách khi đổi/gỡ QL (kèm SĐT QL mới) | Xong — type `MANAGER_CHANGED` |
| 2 — QL mới thấy việc đón khách gấp | Xong |
| 3 — QL cũ biết còn mấy khu vực + `GET /managers/idle` | Xong |
| 4 — `POST /zones/manager-transfer` một transaction | Xong |

Dữ liệu gán/gỡ khu vực **không đổi** (vẫn chỉ đụng zone đích).

**FE:** thông báo khách do BE tạo. Tick “chuyển hẳn” nên gọi `manager-transfer`. Banner QL rảnh có thể dùng `/managers/idle`.

---

## 3. Chặn khai thác trước ngày HĐ chủ nhà

File: `BE-DONE-chan-khai-thac-truoc-ngay-hop-dong-2026-08-19.md`

| Việc | Kết quả |
|---|---|
| Rule 4 — `confirmContract` không đón khách trước `lease.startDate` | Xong |
| Rule 1–2 — tạo HĐ (kể cả nháp + import) | Xong |
| Rule 3 — sửa nháp không lách ngày | Xong |
| Rule 5 — không kích hoạt nhà HĐ chủ nhà đã hết | Xong |
| Cảnh báo 6–8 + `leaseStartDate`/`leaseEndDate` trên GET property | Xong |

**FE:** form HĐ khách đọc `GET /properties/{id}` (`leaseStartDate`, `leaseEndDate`). Host duyệt giá: cờ `leaseNotStartedWarning`, không chặn cứng.

---

## 4. Import Excel số thập phân nhân 10

File: `BE-DONE-import-excel-so-thap-phan-nhan-10-2026-08-19.md`

Ô numeric 29.6 trên JVM `vi-VN` từng thành 296. Đọc thẳng `getNumericCellValue()`, formatter `Locale.US`, parse chuỗi linh hoạt. **Dữ liệu đã import vẫn sai trong DB** — cần import lại.

---

## 5. Định giá đếm sai số tháng

File: `BE-DONE-dinh-gia-dem-sai-so-thang-2026-08-19.md`

Mẫu số chia vốn = tháng còn khai thác (sau cải tạo / hôm nay) trừ 1 tháng bàn giao (HĐ ≥ 6 tháng). `suggestedMinPrice` / `roomFloor` theo số mới. Cần **tính lại giá**. Response có `leaseMonths`, `rentableFrom`, `rentableMonths`, `handoverBufferMonths`, `revenueMonths`.
