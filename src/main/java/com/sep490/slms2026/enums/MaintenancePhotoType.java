package com.sep490.slms2026.enums;

/** Loại ảnh trong lịch sử bảo trì (append-only log). */
public enum MaintenancePhotoType {
    BEFORE,
    FAULT_EVIDENCE,
    SELF_REPAIR,
    AFTER,
    INVOICE
}
