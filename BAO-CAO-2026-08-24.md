# Báo cáo BE — 24/08/2026

---

## 1. Nhà chia phòng có cải tạo — xong cải tạo ACTIVE nhưng phòng kẹt DRAFT

Nguồn: `BE-BUG-cai-tao-xong-khong-mo-phong-2026-08-24.md`

**Đính chính:** không phải BE thiếu hẳn bước mở phòng. Ba đường `hostConfirm` / `confirmPerRoom` / `assignOperationManager` (heal) đã gọi `activateDraftRoomsPerRoom`. Chỉ nhánh `completeRenovation` khi có `operationManagerId` set `ACTIVE` mà **không** mở phòng.

### 1.1 Vá `completeRenovation`

**File:** `PropertyOnboardingServiceImpl`

Sau `save`, nếu nhà `ACTIVE`:

- nguyên căn → `activateDraftRooms`
- chia phòng → `activateDraftRoomsPerRoom(propertyId, true)`

### 1.2 Soft-activate trả phòng bị bỏ qua

**File:** `PropertyActivationResponse`, `PropertyOnboardingServiceImpl`

`activateDraftRoomsPerRoom(..., soft=true)` trước đây chỉ `log.warn`. Nay gắn vào response:

- `skippedRoomCount`
- `skippedRooms[]` — `roomId`, `roomNumber`, `reason` (`Chưa có giá` / `Chưa có kết quả tính giá`)

Áp dụng trên `hostConfirm` và `assignOperationManager` (nhánh heal ACTIVE).

**FE:** hiện cảnh báo tại màn kích hoạt khi `skippedRoomCount > 0`.

### 1.3 Backfill nhà đã dính (MTX#07, MTX#08, …)

Không cần runner mới. Gọi lại `assignOperationManager` với đúng manager hiện tại — nhánh heal chạy `activateDraftRoomsPerRoom` **không** phụ thuộc việc manager có đổi hay không.

---

## 2. Gán quản lý theo khu vực → ACTIVE nhưng không mở phòng

Nguồn: `BE-BUG-gan-quan-ly-theo-khu-vuc-khong-mo-phong-2026-08-24.md`

Đường **thứ năm** đưa nhà sang `ACTIVE`: JPQL bulk `updateOperationManagerByZoneId` lật `PENDING_OPERATION_MANAGER → ACTIVE` dưới DB — không load entity, không mở phòng, không set `managerAcceptedAt`.

### 2.1 Bỏ bulk UPDATE, đi từng nhà

**File:** `ZoneAssignmentServiceImpl`, `PropertyOnboardingService`

- `assignManager` → `propertyOnboardingService.applyZoneOperationManager`
- Mỗi nhà `PENDING_OPERATION_MANAGER` / `ACTIVE` / `RENTED` → `assignOperationManager` (mở phòng, `managerAcceptedAt`, `PROPERTY_ASSIGNED`)
- `MAINTENANCE` / `INACTIVE` chỉ cập nhật `operationManagerId`
- Xóa JPQL `updateOperationManagerByZoneId` / `removeOperationManagerByZoneId`

### 2.2 Gỡ QL khu vực

`releaseZoneOperationManager`: nhà `ACTIVE` → `PENDING_OPERATION_MANAGER`, xóa `managerAcceptedAt`, phòng trống `AVAILABLE` → `DRAFT`.

### 2.3 Gom mở phòng

Private `openDraftRoomsIfActive` — hostConfirm / assign / completeRenovation / gán khu vực đều gọi một chỗ.

**Backfill:** môi trường đã chữa bằng `POST /properties/{id}/operation-manager`. Môi trường khác còn dính: gọi lại với manager hiện tại cho nhà `ACTIVE` còn phòng `DRAFT`.

---

## 3. Thanh lý HĐ không khoá tài khoản khách (vòng đời cọc)

Nguồn: `BE-NEED-khong-khoa-tai-khoan-khach-khi-thanh-ly-2026-08-24.md`

### 3.1 Bỏ khoá lúc `terminateActiveContract`

Thanh lý vẫn giải phóng phòng / set `TERMINATED`. Không gọi `disableTenantAccountIfNoActiveContracts` — khách còn cần `confirmRefund` / `disputeRefund`.

`cancelContract` / `deleteContract` / no-show auto-cancel giữ khoá như cũ (không phải luồng hoàn cọc).

### 3.2 Khoá theo vòng đời cọc (bản gọn — chưa làm chỉ-đọc)

| Mốc | Hành vi |
|---|---|
| Khách `confirmRefund` | `DISABLE` nếu không còn HĐ sống khác (bỏ qua HĐ checkout hiện tại) |
| Host đã `refundPaidAt`, khách im lặng 30 ngày (không confirm / không dispute) | cron `00:15` → cùng logic khoá |
| Khách `disputeRefund` | giữ đầy đủ quyền |

API: `TenantOnboardingService.disableTenantAccountIfNoActiveContracts(userId, exceptContractId)`.

**FE:** cổng tạm “Hoàn tất cần refundPaidAt” có thể nới lại — thanh lý không còn khoá TK.

---

## 4. Cổng tiền thanh lý HĐ — đưa lên BE

Nguồn: `BE-BUG-cong-tien-khi-thanh-ly-chi-ton-tai-o-FE-2026-08-24.md`

`completeRequest` trước đây chỉ kiểm status / HĐ — không đối chiếu nợ. FE fail-open khi `getSettlement` lỗi.

### 4.1 Guard trong `completeRequest`

**File:** `TenantCheckoutServiceImpl`

- Có bảng quyết toán + dòng hoá đơn khoá → chỉ cho hoàn tất khi các hoá đơn đó đã `PAID` (hoặc không còn trong danh sách chưa thu).
- Chưa có bảng / chưa có dòng → kiểm mọi hoá đơn HĐ chưa `PAID`/`CANCELLED`.
- Còn nợ → `422` / `BusinessException`: `"Khách còn nợ {amount} — không thanh lý được hợp đồng khi chưa thu đủ."`

Không gắn `refundPaidAt` vào cổng này (đã tách khoá TK khỏi thanh lý ở mục 3).

### 4.2 `GET .../settlement` trả 404 rõ ràng

**File:** `CheckoutProcessServiceImpl.getSettlement`

- Không tìm thấy yêu cầu / chưa có biên bản kiểm tra → `ResourceNotFoundException` (**HTTP 404**), không còn `RuntimeException` → 500.
- Hồ sơ có kiểm tra thì luôn **200** kèm quyết toán tính trên chỗ (không phụ thuộc entity `CheckoutSettlement` đã khoá).

**FE:** `settlementMissing` chỉ khi **404**; 5xx / mạng lỗi thì báo lỗi, không mở nút Hoàn tất. BE đã chặn nợ nên fail-open FE không còn nguy hiểm thuần nghiệp vụ.
